using System;
using System.Collections.Generic;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Checkers;

namespace BrainTest
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {

            /*CheckerBrain testBrain2 = new CheckerBrain();
            Board b = new Board();
            Console.WriteLine(b.toString());
            b.Invert();
            Console.WriteLine(b.toString());
           
            double output2 = testBrain2.AssessPotentialMove(b);
            
            Console.WriteLine("brain 2: {0}\n", output2);
            testBrain2.Mutate(3);
            List<Board> possibleMoves = GameSim.GetPossibleMoves(b);
            b = testBrain2.DecideNextMove(possibleMoves);
            Console.WriteLine("{0} moves found, selected:", possibleMoves.Count);
            Console.WriteLine(b.toString());
            b.Invert();
            Console.WriteLine(b.toString());
            possibleMoves = GameSim.GetPossibleMoves(b);
            b = testBrain2.DecideNextMove(possibleMoves);
            Console.WriteLine("{0} moves found, selected:", possibleMoves.Count);
            Console.WriteLine(b.toString());
            possibleMoves = GameSim.GetPossibleMoves(b);
            b = testBrain2.DecideNextMove(possibleMoves);
            Console.WriteLine("{0} moves found, selected:", possibleMoves.Count);
            Console.WriteLine(b.toString());
            possibleMoves = GameSim.GetPossibleMoves(b);
            b = testBrain2.DecideNextMove(possibleMoves);
            Console.WriteLine("{0} moves found, selected:", possibleMoves.Count);
            Console.WriteLine(b.toString());*/

           
            //Trainer t = new Trainer();
            //t.Train();
            
            //Console.ReadKey();
        }
    }
}
